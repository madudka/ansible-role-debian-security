---
- name: backup sshd config
  copy:
    src: /etc/ssh/sshd_config
    dest: /etc/ssh/sshd_config.backup
    remote_src: yes
  become: true

- name: change ssh port
  command: sed -i 's/#Port {{ ssh_port }}/Port {{ ssh_port_custom }}/' /etc/ssh/sshd_config
  notify: restart sshd 
  become: true